import { BigNumberish } from "@ethersproject/bignumber";
import { BaseBuilder, BaseBuildParams } from "../base";
import { Order } from "../../order";
interface BuildParams extends BaseBuildParams {
    contract: string;
    tokenIds: BigNumberish[];
}
export declare class TokenListErc721Builder extends BaseBuilder {
    constructor(chainId: number);
    getMerkleRoot(order: Order): string | undefined;
    isValid(order: Order): boolean;
    build(params: BuildParams): Order;
    buildMatching: (order: Order, taker: string, tokenId: string, tokenIds: string[]) => Order;
}
export {};
//# sourceMappingURL=erc721.d.ts.map