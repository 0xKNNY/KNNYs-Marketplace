"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const colorKeys = [
    '50',
    '100',
    '200',
    '300',
    '400',
    '500',
    '600',
    '700',
    '800',
    '900',
];
function isColor(color) {
    if (typeof color !== 'object')
        return false;
    return Object.keys(color).some((key) => colorKeys.includes(key) && typeof color[key] === 'string');
}
function getColors(config) {
    const configValues = config();
    const TailwindColors = configValues['theme']['colors'];
    const colors = {};
    Object.keys(TailwindColors).forEach((key) => {
        if (isColor(TailwindColors[key])) {
            colors[key] = TailwindColors[key];
        }
    });
    return Object.keys(colors);
}
exports.default = getColors;
